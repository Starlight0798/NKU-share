两个文件夹分别是接收端和发送端，可执行文件和代码均在其中。

UDPServer: 接收端，默认把接收文件命名成received_file.bin
UDPClient: 发送端，4个测试文件也在这个目录下，先运行接收端再运行发送端输入发送即可

注意，我并没有使用路由器程序，我在接收端(服务端)模拟丢包和延时，在编译时设置的丢包和延时参数：
丢包率：3 %
延时：50 ms   (随机选取数据包进行延时，并非每个包均延时)
数据包大小：10000 字节